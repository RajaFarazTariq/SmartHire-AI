# =============================================================================
# SmartHire-AI — Dependency Reference (requirements.txt)
# =============================================================================
# NOTE: This is a Node.js / Next.js (TypeScript) project. The AUTHORITATIVE
# dependency manifest is `package.json`. Install everything with:
#
#     npm install
#
# This file is a human-readable, categorized mirror of those dependencies
# (name==version) for quick scanning and onboarding. It is NOT pip-installable.
# Versions reflect the minimums declared in package.json (semver `^`).
# =============================================================================

# ── Runtime / Environment ────────────────────────────────────────────────────
node>=20.0.0                      # JavaScript runtime (Next.js 15 requires >=18.18)
npm>=10.0.0                       # Package manager

# ── Core Framework ───────────────────────────────────────────────────────────
next==15.5.18                     # React framework (App Router, RSC, middleware)
react==19.2.6                     # UI library
react-dom==19.2.6                 # React DOM renderer
typescript==5.x                   # Typed JavaScript

# ── Authentication ───────────────────────────────────────────────────────────
@clerk/nextjs==6.39.3             # Auth, sessions, Organizations + RBAC
@clerk/themes==2.4.57             # Clerk component theming (dark/light)

# ── Database / ORM ───────────────────────────────────────────────────────────
@prisma/client==5.10.0            # Prisma runtime client
prisma==5.10.0                    # Prisma CLI + migrations (devDependency)
# Database engine: PostgreSQL (Neon serverless) — external service, see DEPENDENCIES.md

# ── AI / ML Integrations ─────────────────────────────────────────────────────
@google/generative-ai==0.13.0     # Google Gemini (skill extraction + embeddings)
@pinecone-database/pinecone==2.2.0 # Pinecone vector DB (semantic candidate search)

# ── File Storage ─────────────────────────────────────────────────────────────
@vercel/blob==2.4.0               # Private blob storage for resume files

# ── Document Parsing ─────────────────────────────────────────────────────────
pdf-parse==1.1.1                  # Extract text from PDF resumes
mammoth==1.6.0                    # Extract text + render HTML from DOCX resumes

# ── UI Primitives (Radix UI / shadcn-ui base) ────────────────────────────────
@radix-ui/react-alert-dialog==1.1.15
@radix-ui/react-avatar==1.1.11
@radix-ui/react-dialog==1.1.15
@radix-ui/react-dropdown-menu==2.1.16
@radix-ui/react-label==2.1.8
@radix-ui/react-select==2.2.6
@radix-ui/react-separator==1.1.8
@radix-ui/react-slot==1.2.4
@radix-ui/react-tooltip==1.2.8

# ── UI Utilities ─────────────────────────────────────────────────────────────
lucide-react==0.471.0             # Icon set
class-variance-authority==0.7.1   # Variant-based component styling
clsx==2.1.1                       # Conditional className helper
tailwind-merge==3.6.0             # Merge/dedupe Tailwind classes

# ── Styling System ───────────────────────────────────────────────────────────
tailwindcss==4.x                  # Utility-first CSS (v4)
@tailwindcss/postcss==4.x         # Tailwind v4 PostCSS plugin
tw-animate-css==1.4.0             # Animation utilities
next-themes==0.4.6                # Dark/light theme switching

# ── Charts / Data Visualization ──────────────────────────────────────────────
recharts==2.15.4                  # Charts (pipeline, score distribution, trend)

# ── Animation ────────────────────────────────────────────────────────────────
motion==12.40.0                   # Framer Motion (transitions, micro-interactions)

# ── Forms / Upload ───────────────────────────────────────────────────────────
react-dropzone==14.0.0            # Drag-and-drop resume upload

# ── Validation ───────────────────────────────────────────────────────────────
zod==3.23.0                       # Schema validation (forms, AI output)

# ── Notifications ────────────────────────────────────────────────────────────
sonner==2.0.7                     # Toast notifications

# ── Tooling (devDependencies) ────────────────────────────────────────────────
eslint==9.x                       # Linter
eslint-config-next==15.5.18       # Next.js ESLint config
dotenv-cli==11.0.0                # Loads .env.local for Prisma CLI commands
@types/node==20.x                 # Node type definitions
@types/react==19.2.15             # React type definitions
@types/react-dom==19.2.3          # React DOM type definitions
